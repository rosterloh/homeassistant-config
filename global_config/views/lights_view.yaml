# Lights View

# Header
lights_header:
#  title: Lights
#  icon: fal:lamp-floor

# Button Sorting (you can choose either between: domain, entity_id, name, state, last_changed, last_updated or last_triggered.)
lights_sorting:
#  method: name

# Popup (change this number whenever an entity with a brightness attribute shows a switch instead of a slider).
# When the supported features of the entity is larger than the treshold, you'll see a slider. if it is equal or lower, you'll see a switch.
lights_popup:
#  supportedFeaturesTreshold: 9

# Lock (puts a lock on buttons and you will have to press twice, the first tap will unlock, the second tap will toggle the button.)
lights:
#  living_room_lock: false
#  bedroom_lock: true
#  office_lock: false
#  kitchen_lock: false
#  hallway_lock: false
#  other_lock: false

# Config
# The lights config is done through a template and groups. To make the lights view work, make sure that you have filled the hki_groups.yaml
# file. Without those groups this view will not work!