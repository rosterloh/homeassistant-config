title: Floorplan
path:  floorplan
cards:    
  - type: vertical-stack
    cards:
      - type: custom:decluttering-card
        template: quick-access-menu
        variables:
          - name1: Location
          - icon1: map
          - path1: location
      - type: custom:decluttering-card
        template: header
        variables:
          - content: '## Floorplan'
      - type: vertical-stack
        cards:
          - type: custom:decluttering-card
            template: header
            variables:
              - content: '###### Tap elements to trigger actions'
          - type: horizontal-stack
            cards:
              - !include ../includes/blank-card.yaml
              - type: picture-elements
                style: |
                  ha-card {
                    border-radius: var(--border-radius);
                  }
                aspect_ratio: 1.78
                image: /local/img/floorplan.png
                elements:
              - !include ../includes/blank-card.yaml